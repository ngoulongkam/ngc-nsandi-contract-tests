microservice {
  services {
    auth-login-api {
      host = auth-login-api.protected.mdtp
      port = 443
    }

    help-to-save-proxy {
      host = help-to-save-proxy.public.mdtp
      port = 80
    }
  }
}
